var moment = require('moment');
console.log("Omer changed a file");
console.log(moment().format("ddd, hA"));

/*
--save at the end of npm install will save to package.json dependencies.
--save-dev will save to dev-dependencies, like when we want to install jasmine-node.
*/
